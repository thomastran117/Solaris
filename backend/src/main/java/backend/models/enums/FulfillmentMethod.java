package backend.models.enums;

public enum FulfillmentMethod {
    /** Default — shipped to the customer's address by a carrier. */
    DELIVERY,
    /** Customer collects from a STORE / HYBRID location. */
    PICKUP
}
