package backend.models.enums;

/** Customer's preferred time window for a scheduled delivery. */
public enum DeliveryWindow {
    /** Roughly 8am–12pm. */
    MORNING,
    /** Roughly 12pm–5pm. */
    AFTERNOON,
    /** Roughly 5pm–9pm. */
    EVENING
}
